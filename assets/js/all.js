


// ====================
// Header Scroll Effect
// ====================

var header = document.querySelector('header');
var headerNav = document.querySelectorAll('a.nav');



window.addEventListener('scroll', function() {

  if (document.title === "Ye Shin • Dropmark") {

    if (window.innerWidth >= 650) {

      if (window.pageYOffset >= 640) {

        header.style.backgroundColor = "white";

        for (i = 0; i < headerNav.length; i++) {

          headerNav[i].style.color = "#111111";

        }

      } else {

          header.style.backgroundColor = "#2D2B6C";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "white";

          }

        }

      } else {

        if (window.pageYOffset >= 440) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#2D2B6C";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "white";

            }

          }

        }

  } else if (document.title === "Ye Shin • Venmo Groups") {

    if (window.innerWidth >= 650) {

      if (window.pageYOffset >= 640) {

        header.style.backgroundColor = "white";

        for (i = 0; i < headerNav.length; i++) {

          headerNav[i].style.color = "#111111";

        }

      } else {

          header.style.backgroundColor = "#3C95CD";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "white";

          }

        }

      } else {

        if (window.pageYOffset >= 440) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#3C95CD";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "white";

            }

          }

        }

  } else if (document.title === "Ye Shin • Groups Redesign") {

    if (window.innerWidth >= 650) {

      if (window.pageYOffset >= 640) {

        header.style.backgroundColor = "white";

        for (i = 0; i < headerNav.length; i++) {

          headerNav[i].style.color = "#111111";

        }

      } else {

          header.style.backgroundColor = "#F65858";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "white";

          }

        }

      } else {

        if (window.pageYOffset >= 440) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#F65858";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "white";

            }

          }

        }

  } else if (document.title === "Ye Shin • Mindfull") {

    if (window.innerWidth >= 650) {

      if (window.pageYOffset >= 640) {

        header.style.backgroundColor = "white";

        for (i = 0; i < headerNav.length; i++) {

          headerNav[i].style.color = "#111111";

        }

      } else {

          header.style.backgroundColor = "#0173FF";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "white";

          }

        }

      } else {

        if (window.pageYOffset >= 440) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#0173FF";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "white";

            }

          }

        }

  } else if (document.title === "Ye Shin • Starbucks Redesign") {

      if (window.innerWidth >= 650) {

        if (window.pageYOffset >= 640) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#34B881";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "white";

            }

          }

        } else {

          if (window.pageYOffset >= 440) {

            header.style.backgroundColor = "white";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "#111111";

            }

          } else {

              header.style.backgroundColor = "#34B881";

              for (i = 0; i < headerNav.length; i++) {

                headerNav[i].style.color = "white";

              }

            }

          }

  } else if (document.title === "Ye Shin • Poster Designs") {

      if (window.innerWidth >= 650) {

        if (window.pageYOffset >= 640) {

          header.style.backgroundColor = "white";

          for (i = 0; i < headerNav.length; i++) {

            headerNav[i].style.color = "#111111";

          }

        } else {

            header.style.backgroundColor = "#FAFAFA";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "#111111";

            }

          }

        } else {

          if (window.pageYOffset >= 440) {

            header.style.backgroundColor = "white";

            for (i = 0; i < headerNav.length; i++) {

              headerNav[i].style.color = "#111111";

            }

          } else {

              header.style.backgroundColor = "#FAFAFA";

              for (i = 0; i < headerNav.length; i++) {

                headerNav[i].style.color = "#111111";

              }

            }

          }

        }

}, false);
